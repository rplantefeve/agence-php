{% extends "::base.html.twig" %}

{% block title %}RocketfireAgenceMainBundle:Adresse:showAdresse{% endblock %}

{% block body %}
    <h1>Adresse</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse.idAdd }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ adresse.adresse }}</td>
            </tr>
            <tr>
                <th>Code Postal</th>
                <td>{{ adresse.codePostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ adresse.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ adresse.pays }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('adresse_list') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('adresse_edit', { 'idAdd': adresse.idAdd }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aeroport</h1>

    <table>
        <tbody>
            <tr>
                <th>Idaero</th>
                <td>{{ aeroport.idAero }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ aeroport.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('aeroport_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('aeroport_edit', { 'id': aeroport.idAero }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
